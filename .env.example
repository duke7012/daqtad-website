# Publishable values from Supabase → Project Settings → API keys.
# These are safe to expose: the database only allows public reads
# (and song requests while an event has requests open).
SUPABASE_URL=https://yourproject.supabase.co
SUPABASE_ANON_KEY=sb_publishable_xxxxxxxx
